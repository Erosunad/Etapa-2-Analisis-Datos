Parte 1:

#Se cargan las librerias necesarias

import matplotlib.pyplot as plt
from sklearn import  linear_model #Para saber valores optimos
import seaborn as sns
import numpy as np
import pandas as pd

Seleccionar y ejecutar

Parte 2

#Se carga la información del archivo CSV

datos = pd.read_csv("C:\Users\tonch\OneDrive\Escritorio\T Edna\Future50.csv", sep =",")

Seleccionar y ejecutar

Parte 3
#Se visualizan los datos

data

Seleccionar y ejecutar

******************************

Parte 4

#Exploración de los datos

datos.columns
datos.info
datos.describe

Seleccionar y ejecutar

Parte 5

#Agrupar datos

datos.groupby(['Franchising'])

Seleccionar y ejecutar

Parte 6

#Realizar grafica de dispersion

datos.plot.scatter(x="Sales", y="Units")
plt.show()

Seleccionar y ejecutar

Parte 7

#Regresión lineal

regresion = linear_model.LinearRegression()

Seleccionar y ejecutar

Parte 8 

#Se agregan los datos a un array

ventas = datos["Sales"].values.reshape((-1,1))

Seleccionar y ejecutar

Parte 9 

#Se crea el modelo

modelo = regresion.fit(ventas, datos["Units"])

#Se imprime interseccion

print("Interseccion (b)", modelo.intercept_)

#Se imprime pendiente

print("Pendiente (m)", modelo.coef_)

Seleccionar y ejecutar

*********************************

Parte 10

entrada = [[21],[24],[39],[44]]
predicciones = modelo.predict(entrada)
print(predicciones)

Seleccionar y ejecutar

Parte 11

datos.plot.scatter(x="Sales", y="Units", label= 'Información Restaurantes')
plt.scatter(entrada, predicciones, color='green')
plt.plot(entrada, predicciones, color='red', label='Linea de Regresión')
plt.xlabel('Sales')
plt.ylabel('Units')
plt.legend()
plt.show()



